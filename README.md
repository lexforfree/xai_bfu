## to install uv
```bash
curl -LsSf https://astral.sh/uv/0.9.26/install.sh | sh
pip install uv
sudo port install uv
cargo install --locked uv
```


## to instal env

```bash
uv sync
```

## to run notebook 

```bash
uv run jupyter lab
```